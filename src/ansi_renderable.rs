use {
    crate::{Color, Contrast, Layer, Result},
    std::fmt::{Debug, Display},
};
// pub trait AnsiRenderable: Sized + Clone + Debug + Display {
//     fn render(&self) -> String;
// }

// pub trait AnsiRenderableWithColor: Sized + Clone + Debug + Display {
//     fn render(&self, color: Color) -> String;
// }

// pub trait AnsiRenderableWithColorAndLayer: Sized + Clone + Debug + Display {
//     fn render(&self, color: Color, layer: Layer) -> String;
// }
